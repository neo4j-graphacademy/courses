= Select In Code Block
:order: 3

Add a comment with `/*select` at the start:

[source,cypher]
----
MATCH (m:Movie)/*select*/(p:Person)
RETURN m.title, p.name
----

Provide multiple choice in a checklist, marking the correct answer with an `[x]`

[source,adoc]
* [ ] `-[:ACTED_IN]->`
* [x] `<-[:ACTED_IN]-`

include::questions/select-in-code.adoc[]