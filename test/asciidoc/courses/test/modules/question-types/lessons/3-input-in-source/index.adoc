= Input In Code Block
:order: 3

Add a comment with `/*input` at the start:

[source,cypher]
----
MATCH (m: /*input*/ {title: "The Matrix")<-[:ACTED_IN]-(p:Person)
RETURN m.title, p.name
----

Provide a checklist with a single option: the correct answer marked with an `[x]`

[source,adoc]
* [x] Movie:Comedy

include::questions/input-in-code.adoc[]