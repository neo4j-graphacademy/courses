[.verify]
= Actor and Director labels

Once you had added the `Actor` and `Director` labels, click the **Check Database** button to verify that the task is complete.

[TIP,role=hint]
.Hint
====
Run the Cypher query to add the `Actor` labels to the graph. Modify the query to add the `Director` labels for `Person` nodes that have a `DIRECTED` relationship to a `Movie`.
====

[TIP,role=solution]
.Solution
====

Run this Cypher statement to add the `Actor` and `Director` labels to the graph.

[source,cypher]
----
include::../solution.cypher[]
----

====
