[.verify]
= Validate Person dates

Once you have updated the `Person` nodes to use `Date` for `born` and `death` properties, click the **Check Database** button to verify that the task is complete.

verify::[]

//appears when user clicks the Hint button
[TIP,role=hint]
.Hint
====
Run the supplied Cypher to update the `Person` nodes using the `date` function.
====

[TIP,role=solution]
.Solution
====

Run this Cypher statement to update the `Person` nodes using the `date` function.

[source,cypher]
----
include::../solution.cypher[]
----

====
