[.verify]
= Validate Results

Once you have imported the `Person` nodes, click the **Check Database** button to verify that the task is complete.


verify::[]

//appears when user clicks the Hint button
[TIP,role=hint]
.Hint
====
Run the Cypher statement that loads the CSV file and creates the `Person` nodes.
====

[TIP,role=solution]
.Solution
====

Run this Cypher statement to create `Person` nodes:

[source,cypher]
----
include::../solution.cypher[]
----

====
