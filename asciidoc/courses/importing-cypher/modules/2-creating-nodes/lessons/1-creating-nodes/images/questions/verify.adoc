[.verify]
= Validate Results

Once you have created the `Person` nodes, you can click the **Check Database** button to verify that the task is complete.


verify::[]

[TIP,role=hint]
.Hint
====
Run the Cypher statement to load the `person.csv` file and create the `Person` nodes.
====

[TIP,role=solution]
.Solution
====

Run this Cypher statement to load the `person.csv` file and create the `Person` nodes.

[source,cypher]
----
include::../solution.cypher[]
----

====
