// TODO - Check with adam if I can have 2 verify questions, 1 for acted in, 1 for directed

[.verify]
= Validate relationships

Once you have created `ACTED_IN` and `DIRECTED` relationships, click the **Check Database** button to verify that the task is complete.

verify::[]

//appears when user clicks the Hint button
[TIP,role=hint]
.Hint
====
Run the supplied Cypher to create the `ACTED_IN` relationships.

Create and run a modified Cypher statement for the `DIRECTED` relationships, which:

* Uses the `directed.csv` file
* Uses `MERGE` to create the directed relationship.
====

[TIP,role=solution]
.Solution
====

Run this Cypher statement to create the `ACTED_IN` AND `DIRECTED` relationships:

[source,cypher]
----
include::../solution.cypher[]
----

====
