= Importing Category Nodes
:type: challenge
:order: 8
:duration: 10
:optional: true

[.slide.discrete]
== Importing Category Nodes

You imported Product, Customer, and Order nodes. Now import Category nodes using the same workflow.

In this challenge, you will work independently to import Category nodes from the categories.csv file.

[.slide]
== Map the Category node

In the **Data Modeling tab**:

. Select the **Define manually** option. This will open the data modeling canvas with a new node.
. **Set the label:** With the new node selected, under **Definition**, set the label to `Category`
. **Select the table:** Under **Table**, select `categories.csv`


[.slide]
== Configure properties

Use the **Map from table** button to select the following columns. Verify their types and rename as applicable:

[options="header"]
|===
| Column | Type | Rename To
| categoryID | Integer | id
| categoryName | String | name
| description | String |
|===

[NOTE]
.ID Property
====
The data importer will attempt to automatically select the ID property.

Verify the ID property is set to `id` and update if necessary.
====


[.slide]
== Run the import

Click the **Run Import** button to import the data. You will see a progress indicator while the import is running.

Once the import is complete, you will see a summary of the import, including the Cypher statements that were executed during the import process.

You should see confirmation that **8 Category nodes** were created.

include::questions/verify.adoc[leveloffset=+1]

[.summary]
== Summary

You imported Category nodes into your graph.

* Created 8 Category nodes
* Mapped properties from categories.csv
* Set categoryId as the unique identifier

You now have all the node types needed for Module 2.
