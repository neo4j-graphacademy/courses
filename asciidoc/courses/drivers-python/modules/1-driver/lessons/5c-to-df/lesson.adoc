= Using result transfomers 
:type: quiz
:minutes: 5
:order: 5

You are working within a Jupyter notebook and want to transform the result of a query into a pandas `DataFrame`.

include::questions/1-result-transformer.adoc[leveloffset=+1]

[.summary]
== Lesson summary

Nice work! You can now transform the result of a query into a pandas `DataFrame`.

You now have the tools needed to run simple Cypher statements and transform the results where needed.

In the next module, we will explore how to execute more complex Cypher statements.

