= Writing Data to Neo4j
:type: challenge

In the window to the right, we have set up a codesandbox /??/ window with a driver already configured to connect to a Neo4j Sandbox instance.

Your challenge is to add yourself as an actor in The Matrix but executing the Cypher statement in the `cypher` variable in a write transaction.


include::questions/verify.adoc[leveloffset=+1]

[.summary]
== Lesson Summary

In this challenge, you used your knowledge to create a driver instance and run a Cypher statement.

Next, we will look at the Neo4j Type System and some of the considerations that you need to make when working with values coming from Neo4j in your TypeScript application.
