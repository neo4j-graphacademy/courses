= Oldest actor
:type: challenge
:sandbox: true

include::./questions/q1.adoc[leveloffset=+1]

[.summary]
== Summary

In this challenge, you wrote a query that uses `head()`  to return the oldest actor for a given year.

In the next challenge, you will write a query to perform a calculation on all elements in a list.