[.question]
= 2. Aggregating data

What Cypher functions can you use to return the number of elements in a list of Movie nodes, m?

* [x] size(collect(m))
* [ ] size(m)
* [x] count(m)
* [ ] count(collect(m))

