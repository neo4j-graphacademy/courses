= Optional Library Practice
:type: lesson
:order: 4
:duration: 10
:optional: true
:sandbox: true


[.slide.discrete]
== Optional Practice

You built the outline and search tools.
In this lesson, you will practice the model underneath them - the `HAS` tree, the URIs, and the link types - with four more questions from the AutoFix shop floor.

**Advanced learners:** Skip to Module 4.

**Beginners:** These exercises build the traversal fluency the themes shape and the estate questions rely on.


[.slide]
== Exercise 1: Size up the estate

Which document has the most sections, and how deep does each tree go?

[%collapsible]
====
[source,cypher]
.Sections per document
----
MATCH path = (d:Document)-[:HAS*]->(s:Section)
RETURN d.displayName AS document,
       count(s) AS sections,
       max(length(path)) AS maxDepth
ORDER BY sections DESC
----

Counting over the variable-length pattern gives the size of each tree, and the longest path gives its depth - manuals nest deepest (reaching depth 3 below the document), while bulletins and recalls stay at depth 1.

**Try experimenting:**

* Add `WHERE d.uri STARTS WITH 'technical-library/bulletins'` to size up only the bulletins
* Start the walk from a `Folder` instead, and watch the depths shift by one
====


[.slide]
== Exercise 2: Read around a section

A technician is reading the Falcon manual's misfire diagnosis.
What comes immediately before and after it, in reading order?

[%collapsible]
====
[source,cypher]
.The reading-order window
----
MATCH (s:Section {uri: 'technical-library/manuals/man-falcon-engine.pdf#misfire-diagnosis'})
OPTIONAL MATCH (prev)-[:NEXT_SECTION]->(s)
OPTIONAL MATCH (s)-[:NEXT_SECTION]->(next)
RETURN prev.displayName AS before, s.displayName AS here, next.displayName AS after
----

`NEXT_SECTION` threads the document the way a human reads it - so an agent can widen its context window around any hit by one hop in each direction, without re-deriving order from the tree.

**Try experimenting:**

* Chain two hops: `(s)-[:NEXT_SECTION*1..2]->(n)` for a wider window
====


[.slide]
== Exercise 3: Where the links land

`LINKS_TO` is the cross-reference the author actually wrote.
A link can land on a specific `Section` (most do - "see this exact procedure") or on a whole `Document` ("see the Heron manual").
How many of each kind are there?

[%collapsible]
====
[source,cypher]
.What do links point at?
----
MATCH (:Section)-[:LINKS_TO]->(t)
RETURN CASE WHEN t:Section THEN 'section' ELSE 'document' END AS target,
       count(*) AS links
ORDER BY links DESC
----

Most links target a precise section; the rest point at a document as a whole.
An outbound URL the author wrote becomes a stub `Document {external: true}`, so a link to the outside world looks like any other document target.

**Try experimenting:**

* List the documents one bulletin links to: `MATCH (:Document {id:'tsb-20-501'})-[:HAS*]->(:Section)-[:LINKS_TO]->(t) MATCH (d:Document)-[:HAS*0..]->(t) RETURN DISTINCT d.title, t.displayName`
====


[.slide]
== Exercise 4: Subtree scoping with URIs

Count the sections under each folder - without touching the tree.

[%collapsible]
====
[source,cypher]
.Prefix matching replaces a traversal
----
MATCH (f:Folder)
MATCH (s:Section) WHERE s.uri STARTS WITH f.uri + '/'
RETURN f.name AS folder, count(s) AS sections
ORDER BY sections DESC
----

Because URIs encode the hierarchy, subtree questions can be answered by string prefix alone - the same trick your search tool's `--under` scope and the theme tool's document collapse use.

**Try experimenting:**

* Compare with the traversal form `(f)-[:HAS*]->(s:Section)` - same counts, two different reasoning styles
====

read::Complete practice[]


[.summary]
== Summary

In this optional practice, you exercised the model:

* **Tree aggregation** - counting and depth over `[:HAS*]`
* **Reading order** - `NEXT_SECTION` windows around any section
* **Link targets** - `LINKS_TO` landing on a section versus a whole document
* **URI scoping** - subtree questions as string prefixes

Module 4 builds the themes shape on exactly these pieces.
