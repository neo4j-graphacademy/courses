= Merge the Lakehouse - Paths
:order: 4

The bulletin is in a PDF. The vehicle is in a Delta table. The part number that connects them appears in both - and the load pipeline merged them into one graph on exactly that key.
In this module, you will build the judgment and action tools that traverse it - then hand your agent a live work order and watch it decide, act, and leave a trail.


By the end of this module, you will:

* Explain why MERGE on a shared key is the boundary crossing between documents and tables
* Build what_fixed_this and recall_exposure from their specs - multi-hop evidence across both halves
* Build the action tools: order parts through an API, write grounded recommendations to the graph
* Run your agent on live work orders - including one it should refuse to solve


link:./1-merge-on-shared-keys/[Ready? Let's go →, role=btn]
