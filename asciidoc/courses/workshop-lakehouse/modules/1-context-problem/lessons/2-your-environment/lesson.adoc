= Your Environment
:type: lesson
:order: 2
:duration: 6
:sandbox: true


[.slide.discrete]
== Introduction

You saw the three-layer wall in the previous lesson.
You will break through it the way you would on the job: by building a **service-advisor agent** - a skill your coding agent runs to decide and act on AutoFix work orders.

In this lesson, you will open your workshop environment, connect it to Neo4j, and load the lakehouse.


[.slide]
== Open your Codespace

The workshop environment runs in a GitHub Codespace - a cloud development environment in your browser with everything preinstalled: Python, the Neo4j CLI with its agent skills, and the Claude Code coding agent.

// TODO: repoint to neo4j-graphacademy/genai-workshop-lakehouse once the org repo exists
link:https://github.com/codespaces/new/zach-blumenfeld/genai-workshop-lakehouse[Open in GitHub Codespace^,role="btn"]

While it builds (a few minutes), look at what is inside:

[cols="1,2", options="header"]
|===
| Folder | What
| `sources/` | The lakehouse: real PDF manuals, bulletins, and recall notices, plus CSV exports of the Delta tables
| `load/` | The pipeline that parses the PDFs and merges the warehouse into one graph
| `docs/` | **The shape specs** - outline-format.md and theme-format.md, the contracts you build against
| `skill/` | **What you build** - the service-advisor playbook and its tool scripts
| `api/` | A mock parts-ordering API your agent will act through
| `events/` | Incoming work orders for the finale
| `solutions/` | Complete scripts if you need to catch up
|===

[NOTE]
.Prefer your own setup?
====
Everything runs locally too: clone the repository, `pip install -r requirements.txt`, install link:https://github.com/neo4j-labs/neo4j-cli[neo4j-cli^], and run `neo4j-cli skill install` for the agent of your choice.
====


[.slide]
== Connect to your database

GraphAcademy has provisioned a Neo4j sandbox for you, with the Graph Data Science library you will use in Module 3.
Paste these credentials into the `.env` file in your Codespace:

[source,subs="verbatim,attributes"]
.Your database credentials (.env)
----
NEO4J_URI={instance-scheme}://{instance-ip}:{instance-boltPort}
NEO4J_USERNAME={instance-username}
NEO4J_PASSWORD={instance-password}
NEO4J_DATABASE={instance-database}
----

[NOTE]
.Claude access
====
If you are signed in to Claude Code, you are done.
Otherwise set `ANTHROPIC_API_KEY` in the same `.env` file - use the key provided in the workshop, or create your own at link:https://console.anthropic.com[console.anthropic.com^].
====


[.slide]
== Load the lakehouse

One command builds the workshop's starting graph:

[source,shell]
.Parse the PDFs, merge the warehouse
----
python load/load_graph.py
----

You should see the pipeline parse 10 PDFs into one Library tree - 3 folders, 10 documents, 37 sections with citation and shared-key links - then merge the six warehouse tables into the same graph.
Worth noticing while it runs: the document half comes from **real PDFs** and the warehouse half from CSV exports - and `load/warehouse_source.py` swaps those CSVs for a live Databricks connection by changing one environment variable.
The graph, and everything you build on it, never knows the difference.

Then start the parts API in a second terminal - your agent acts through it in Module 4:

[source,shell]
.Start the mock dealer-management API
----
uvicorn api.parts_api:app --port 8800
----


[.slide]
== Meet your agent

Start your coding agent in the repository root and ask it something only the graph can answer:

[source,shell]
.Smoke test
----
claude
> What is in the Neo4j database described by .env? Give me counts by label.
----

The agent uses the Neo4j skills to inspect the graph: 10 documents, 37 sections, 30 vehicles, 39 work orders - both halves of the lakehouse, one graph.
The sandbox window on the right is your inspection surface throughout: when a tool returns ids, look at them there as a graph.

read::Continue[]


[.summary]
== Summary

In this lesson, you set up your environment:

* **Codespace** - Python, neo4j-cli with agent skills, and Claude Code, preinstalled
* **One-command load** - PDFs parsed, warehouse merged, sources swappable for live Databricks
* **Parts API** - the external system your agent will act through
* **Smoke test** - your agent can already see both halves of the lakehouse

In the next module, you will build your skill's first tools and navigate the document tree.
