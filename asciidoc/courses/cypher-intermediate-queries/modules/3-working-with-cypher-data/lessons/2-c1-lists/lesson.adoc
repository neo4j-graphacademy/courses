= 1-Creating lists
:type: challenge
:sandbox: true

== Creating a list

Write a query to return the title of the movie along with its list of actors.
Order the lists returned so that the movie with the most actors in the graph is first.

Then answer this question:

include::./questions/q1.adoc[leveloffset=+1]

[.summary]
== Summary

In this challenge, you wrote a query to return lists of actors and order the lists to determine the largest list.

In the next challenge, you will answer another question using this same query.

