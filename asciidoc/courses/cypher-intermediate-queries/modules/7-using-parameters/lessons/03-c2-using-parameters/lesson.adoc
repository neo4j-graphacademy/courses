= Using Parameters
:type: challenge
:sandbox: true

== Execute query using parameters

Modify this query so that it utilizes the parameters you set in the previous challenge:

[source,cypher]
----
MATCH (p:Person)-[:ACTED_IN]->(m:Movie)
WHERE p.name STARTS WITH "Tom"
AND "UK" IN m.countries
RETURN p.name AS actor,
m.title AS title
----

After you have modified the query with parameters, answer this question:

include::./questions/q1.adoc[leveloffset=+1]



[.summary]
== Summary

In this challenge, you rewrote a query to use parameters.

In the next lesson, you will see some application code that uses parameters in Cypher.
