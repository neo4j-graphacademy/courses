= Merge on Shared Keys
:type: lesson
:order: 1
:duration: 6


[.slide.discrete]
== Introduction

Your graph knows what the documents say.
It does not yet know what actually happened in the shop - that is in the Delta tables.

In this lesson, you will learn how warehouse tables become nodes and relationships, and why merging on a shared key is the move that crosses the document-to-table boundary.


[.slide]
== Tables become nodes and relationships

Each Delta table maps naturally onto the graph:

[cols="1,2", options="header"]
|===
| Delta table | Becomes
| `autofix.service.vehicle` | `Vehicle` nodes
| `autofix.service.work_order` | `WorkOrder` nodes, plus `HAS_WORK_ORDER`, `DIAGNOSED`, and `PERFORMED` relationships
| `autofix.service.work_order_part` | `REPLACED` relationships with a `qty` property
| `autofix.service.part` | `Part` nodes and `SUPERSEDED_BY` relationships
| `autofix.service.dtc_code` | `DTC` nodes
| `autofix.service.procedure` | `Procedure` nodes
|===

Foreign keys become relationships: the `vin` column on a work order row becomes `(Vehicle)-[:HAS_WORK_ORDER]->(WorkOrder)`.
So far, that is standard warehouse-to-graph import.


[.slide]
== The boundary crossing is one MERGE

Look again at two of those tables.
`Part` and `DTC` nodes **already exist in your graph** - Module 2 created them from part numbers and trouble codes mentioned in PDF sections.

When you load the warehouse with `MERGE` on the same key, Cypher does not create a duplicate - it finds the existing node and enriches it:

[source,cypher]
.The line that joins documents to tables
----
MERGE (p:Part {partNumber: row.part_number})
SET p.name = row.name
----

After this runs, the `Part` node for `IC-2042-B` is referenced by bulletin sections on one side and replaced on work orders on the other.
The document half and the warehouse half are now **one graph**, joined exactly where they always overlapped.

No new pipeline, no entity resolution model - the IDs were already shared.
The lakehouse just never treated that as a link.


[.slide]
== The production import path

In this workshop you load CSV exports to keep the focus on the graph.
In production, the same mapping runs through tooling - the take-home repository includes both paths:

* **Neo4j Data Importer** - visual mapping of files to nodes and relationships; the repository includes the mapping configuration for these tables
* **Spark connector** - Databricks notebooks that write Delta tables to Neo4j as a scheduled job, with the same `MERGE`-on-key semantics

Either way, the modeling decisions you make in the next challenge - which columns are keys, which become relationships - are the part that transfers.


read::Continue to the challenge[]


[.summary]
== Summary

In this lesson, you learned the merge pattern:

* **Tables map to the graph** - rows become nodes, foreign keys become relationships
* **MERGE on a shared key** - `Part` and `DTC` nodes from the PDFs are enriched, not duplicated, joining both halves into one graph
* **Production paths** - Data Importer or the Spark connector run the same mapping at scale

In the next challenge, you will load the warehouse and verify the boundary crossing.
