= Merge the Lakehouse - Paths
:order: 4

The bulletin is in a PDF. The vehicle is in a Delta table. The part number that connects them appears in both - and in this module, you will make that connection real.
You will merge AutoFix's warehouse tables into the document graph and write the multi-hop query that no single tool could answer.


By the end of this module, you will:

* Merge Vehicle, WorkOrder, Part, and Procedure tables into the graph on shared keys
* Explain why MERGE on a shared key is the boundary crossing between documents and tables
* Write multi-hop Cypher from a bulletin to the vehicles it applies to
* Trace supersession chains and recall exposure across both halves of the lakehouse


link:./1-merge-on-shared-keys/[Ready? Let's go →, role=btn]
