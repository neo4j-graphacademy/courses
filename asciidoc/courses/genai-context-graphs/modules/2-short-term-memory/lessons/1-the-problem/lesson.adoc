= The Stateless Agent Problem
:type: lesson
:order: 1
:duration: 4

LLMs have no memory between requests. Every call to the model starts with no knowledge of previous interactions — the model only knows what you include in the current prompt. This is fine for single-turn Q&A, but it fails immediately for real agent tasks.

In this lesson, you will learn what goes wrong without conversation memory and what short-term memory needs to provide.

== What fails without conversation memory

Consider an agent helping a customer service representative:

. The representative asks: "Review Jessica Norris's account for a credit limit increase."
. The agent retrieves the account details and returns them.
. The representative follows up: "What was the last transaction on that account?"

Without memory, step 3 fails because the agent has no record of step 1. "That account" is a reference to context from the first message, but the model has no access to it. The agent must ask the representative to repeat the account holder's name, breaking the conversation flow.

=== How unstructured history injection fails

You can manually inject the full conversation history into each prompt. This provides context — but no structure. The result is an unstructured sequence of messages, not a traversable graph.

With flat text history, you cannot answer queries like:

* "Which messages in this session mentioned Jessica Norris?"
* "Find the message that triggered this reasoning trace."
* "What entities did the agent know about when it made this decision?"

You are re-reading a log, not querying a knowledge graph.

== What short-term memory provides

Short-term memory solves this with three graph-enabled capabilities:

* *Context injection* — the most recent messages are retrieved and injected into each new prompt automatically. The agent always has the current conversation context without manual re-injection.
* *Semantic search* — past messages are embedded and indexed. You can find messages that discuss the same topic even if they use different words.
* *Graph connectivity* — messages are linked to the entities mentioned in them and to the reasoning traces they triggered. You can traverse from a message to what the agent knew and what it decided.

Graph storage enables these capabilities. Relationships connect messages to entities, entities to other entities, and messages to reasoning traces. You can traverse these connections to answer queries that flat text cannot support.

== Understanding the session boundary

Short-term memory is scoped to a session. When the session ends, the conversation history is no longer the agent's active working memory — but the entities extracted from it persist in long-term memory. The session ends; the knowledge remains.

read::Continue[]

[.summary]
== Summary

In this lesson, you learned why short-term memory is necessary:

* *Stateless LLMs* — without memory management, every prompt is a fresh start; multi-turn agent tasks fail
* *More than a log* — short-term memory provides context injection, semantic search, and graph connectivity
* *Session-scoped* — short-term memory is ephemeral, but entity knowledge extracted from it persists to long-term memory

In the next lesson, you will see the graph schema that implements short-term memory.
