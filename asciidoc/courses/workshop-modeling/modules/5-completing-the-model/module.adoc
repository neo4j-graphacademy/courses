= Completing the Model
:order: 5

You have the core recommendation path complete. Now add the remaining Northwind entities to build the full business graph.

These optional lessons let you explore the complete dataset and practice your graph modeling skills.

By the end of this module, you will:

* Import Category nodes and create `IN_CATEGORY` relationships
* Import Supplier nodes and create `SUPPLIES` relationships
* Import Employee nodes with `REPORTS_TO` and `SOLD` relationships
* Query hierarchical data with variable-length paths
* Have the complete Northwind graph model

link:./2-import-suppliers/[Ready? Let's go →, role=btn]
