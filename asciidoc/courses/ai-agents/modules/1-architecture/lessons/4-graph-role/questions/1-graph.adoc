[.question]
= Graph in Agent Architecture

How does Neo4j serve agents?

* [ ] Only as a vector store
* [x] As both knowledge base and memory for structured, connected data
* [ ] Only for logging
* [ ] Only as a cache

[TIP,role=hint]
.Hint
====
The graph stores structured data and relationships. It can persist context and facts.
====

[TIP,role=solution]
.Solution
====
**As both knowledge base and memory for structured, connected data.**

Neo4j stores structured, connected data that agents query with Cypher. It can also store conversational context and facts as memory.
====
