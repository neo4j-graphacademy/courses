extends ../layouts/classroom

block main

  +status(course, completed, next)

  .module
    h1.module-title
      if type && type != 'text'
        span.overline.module-type !{type}
      | !{title}

    article.module-content.doc !{doc}
