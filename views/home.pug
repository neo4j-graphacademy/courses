extends layouts/default
include mixins/card

block main
  h1 Available Courses

  .card-list
    each course in courses
      //- mixin card(title, image, caption, link)
      +card(course.title, course.thumbnail, course.caption, course.link)
      //- pre #{JSON.stringify(course)}

