//- include the following element on the page:
//- if offerTranslation
//-   div#google_translate_element
if offerTranslation || forceTranslation
  script(src="https://translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"  nonce=nonce)
  script(nonce=nonce).
    function googleTranslateElementInit() {
        new google.translate.TranslateElement(
            {pageLanguage: 'en'},
            'google_translate_element'
        );
      }
