apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: graphacademy-dev
bases:
  - ../base
resources:
  - ingress.yaml
  - issuer.yaml
patches:
 - graphacademy-deployment.yaml
configMapGenerator:
  - name: ga-config-map
    envs:
      - .env