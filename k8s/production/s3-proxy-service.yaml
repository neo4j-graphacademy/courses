apiVersion: v1
kind: Service
metadata:
  name: s3-proxy-service
spec:
  type: ExternalName
  externalName: graphacademy.neo4j.com.s3.amazonaws.com
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
        